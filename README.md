# nyc-taxi-mlops

to run the docker image:

first time setup
- docker compose up --build -d
- docker compose up airflow-init

start docker
- docker compose up

stop docker
- docker compose stop

docker full shutdown
- docker compose down
