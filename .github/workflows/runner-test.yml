name: Runner Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: Show machine info
        run: |
          whoami
          hostname
          pwd
          docker --version
          docker compose version